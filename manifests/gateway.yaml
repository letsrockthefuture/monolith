apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: "${app}-gateway"
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: "${container_port}"
      name: http
      protocol: HTTP
    hosts:
    - "*"
